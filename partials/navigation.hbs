{{#foreach navigation}}
    <a class="mdl-navigation__link {{slug}} {{#if current}} current{{/if}}" href="{{url absolute="true"}}" class="">{{{label}}}</a>
    <a class="mdl-button mdl-js-button mdl-button--raised mdl-js-ripple-effect {{slug}} {{#if current}}current{{/if}}" href="{{url absolute="true"}}" class="">{{{label}}}</a>
{{/foreach}}
